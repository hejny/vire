import * as React from 'react';
import './Processing.css';
import { DataModel } from '../../model/DataModel';

export const Processing = (props: { dataModel: DataModel }) => {
    return <div className="Processing">Načítání...</div>;
};
